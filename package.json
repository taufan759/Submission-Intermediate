{
  "name": "peta-bicara",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "deploy": "npm run build && gh-pages -d dist",
    "rebuild": "npm run clean && npm run build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "workbox-build": "^7.0.0"
  }
}
